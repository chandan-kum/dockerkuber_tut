apiVersion: v1
kind: Deployment
metadata:
  name: web-deployment
spec:
  replicas: 2
  selector:
    matchlables:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
       - name: web
         image: nginx:latest
         ports:
          - containerPort: 80          